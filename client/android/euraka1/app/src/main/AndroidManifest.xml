<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eurekakids.euraka1" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Spinnerscreen"
            android:label="@string/sel_center"
            android:theme="@style/AppTheme"/>
        <activity android:name=".Listscreen"
            android:label="@string/list_children"
            android:theme="@style/AppTheme"/>
        <activity android:name=".AddKid"
            android:label="@string/add_kid"
            android:theme="@style/AppTheme"/>
        <activity android:name=".tam_page"
            android:theme="@style/AppTheme"/>

    </application>

</manifest>
